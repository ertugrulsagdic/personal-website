/** @type {import('next').NextConfig} */

const nextConfig = {
    // experimental: {
    //     ppr: 'incremental',
    // },
    // i18n: {
    //     locales: ['en', 'tr'],
    //     defaultLocale: 'en',
    // },
};

export default nextConfig;
